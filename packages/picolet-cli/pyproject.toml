[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "picolet-cli"
version = "0.2.0"
description = "The picolet command-line tool"
requires-python = ">=3.11"
dependencies = [
    "picolet-templates",
    "picolet-testing",
]

[project.scripts]
picolet = "picolet_cli.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["picolet_cli"]

[tool.uv.sources]
picolet-templates = { workspace = true }
picolet-testing = { workspace = true }
